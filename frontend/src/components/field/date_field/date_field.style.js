const styles = theme => ({
  field: {
    width: "100%",
  },
  yearButton: {
    "& .MuiPickersYear-yearButton": {
      fontSize: theme.typography.body1.fontSize,
    },
  },
});

export default styles;
